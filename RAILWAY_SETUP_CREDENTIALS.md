# Настройка Nova Credentials для Railway

## ✅ Что уже сделано:

1. Создан локальный `auth.json` с вашими credentials
2. `auth.json` уже в `.gitignore` (не будет закоммичен)
3. Dockerfile обновлен для использования `auth.json`

## 📋 Два способа настройки:

### Способ 1: Использовать auth.json (уже настроено)

`auth.json` создан локально и будет скопирован в Docker образ при сборке.

**ВАЖНО:** `auth.json` в `.gitignore`, поэтому он НЕ будет в git репозитории. 

Для Railway нужно:
1. Либо временно закоммитить `auth.json` (не рекомендуется, но работает)
2. Либо добавить переменные окружения в Railway (см. Способ 2)

### Способ 2: Использовать переменные окружения в Railway (рекомендуется)

1. **Railway Dashboard** → ваш сервис
2. **Settings** → **Variables**
3. Добавьте:
   ```
   NOVA_USERNAME=maksstepenko@gmail.com
   NOVA_PASSWORD=Hibmyk-jyjby0-duvvyq
   ```

Railway передаст их как build args в Dockerfile.

## 🚀 Что делать сейчас:

### Вариант A: Временно закоммитить auth.json

Если Railway не может использовать переменные окружения как build args:

```bash
# Временно убрать из .gitignore (НЕ РЕКОМЕНДУЕТСЯ для продакшена)
git add backend/auth.json
git commit -m "Add Nova auth.json for Railway deployment (temporary)"
git push
```

**После успешного деплоя удалите auth.json из репозитория:**
```bash
git rm --cached backend/auth.json
git commit -m "Remove auth.json from repository"
git push
```

### Вариант B: Добавить переменные в Railway (лучше)

1. Railway Dashboard → Settings → Variables
2. Добавьте `NOVA_USERNAME` и `NOVA_PASSWORD`
3. Railway должен автоматически использовать их при сборке

## ⚠️ Безопасность:

- ✅ `auth.json` уже в `.gitignore`
- ⚠️ Если закоммитите `auth.json`, удалите его после деплоя
- ✅ Лучше использовать переменные окружения Railway

## 🔍 Проверка:

После настройки:
1. Railway должен успешно скачать Nova
2. Деплой должен пройти без ошибок
3. Проверьте логи: Railway → Deployments → View Logs

